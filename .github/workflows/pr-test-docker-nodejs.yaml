name: Test Docker Node.js

on:
  pull_request:

permissions:
  contents: read

jobs:
  run:
    name: test-docker-nodejs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm run test:docker:nodejs
      - if: always()
        run: npm run test:docker:clean
