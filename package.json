{
  "name": "ydb-orm",
  "description": "YDB simple ORM implementation",
  "author": {
    "name": "Georgy Malkov",
    "email": "i@malkovgv.ru",
    "url": "https://github.com/goshander/ydb-orm"
  },
  "homepage": "https://github.com/goshander/ydb-orm",
  "repository": {
    "url": "https://github.com/goshander/ydb-orm"
  },
  "license": "see in LICENSE",
  "main": "main.js",
  "version": "1.4.0",
  "scripts": {
    "test-debug": "tap -O -T --node-arg=--inspect-brk --bail --jobs=1 --reporter=list --no-coverage test/**/*.spec.js",
    "test": "tap --bail --jobs=1 --reporter=list --no-coverage test/**/*.spec.js",
    "test-docker": "docker-compose --env-file .env.test build test && docker-compose --env-file .env.test run --rm test",
    "test-docker-clean": "docker-compose --env-file .env.test down -v --rmi local"
  },
  "dependencies": {
    "@yandex-cloud/nodejs-sdk": "^2.4.0",
    "fastify-plugin": "^4.2.0",
    "pino": "^8.6.0",
    "ydb-sdk": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^8.24.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^6.0.1",
    "nanoid": "^3.3.4",
    "tap": "^16.3.0"
  }
}